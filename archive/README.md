# Archive — staged for review/deletion

Files here were **not referenced** by any page, menu, layout, or config at the time
of archiving (audited 2026-06-29). They have been moved out of `static/` so they are
**no longer published** to the live site (reducing page/site weight for our
low-bandwidth audience), but are **kept in version control so no content is lost**.

Nothing here is linked from the site. Before permanently deleting any file:
1. Confirm it is not linked from another PDF or an external bookmark you want to keep.
2. If a file is still needed, move it back under `static/files/...` and add a link in content.

## Contents

`archive/files/` mirrors the original `static/files/` structure. Current items
(19 unreferenced PDFs, ~58 MB) include old holiday lists (2023–2025), COVID-era
documents, and several unlinked forms/brochures.

To list them: `find archive/files -name '*.pdf'`
